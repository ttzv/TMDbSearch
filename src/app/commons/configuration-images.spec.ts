import { ConfigurationImages } from './configuration-images';

describe('ConfigurationImages', () => {
  it('should create an instance', () => {
    expect(new ConfigurationImages()).toBeTruthy();
  });
});
